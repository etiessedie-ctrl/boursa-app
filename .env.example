# Configuration pour déploiement cloud - BoursA
# Copiez ce fichier vers .env et ajustez les valeurs

# Django settings
# 🔐 GÉNÉRER UNE SECRET_KEY SÉCURISÉE:
# Exécutez: python generate_secret_key.py
# OU: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-very-long-random-secret-key-here-at-least-50-characters
DEBUG=False
DJANGO_SETTINGS_MODULE=config.settings.prod

# Domaines autorisés (ajustez selon votre hébergeur)
# Pour Render: votre-app.onrender.com
# Pour Heroku: votre-app.herokuapp.com
# Pour Railway: votre-app.up.railway.app
ALLOWED_HOSTS=votre-app.onrender.com,localhost,127.0.0.1

# Database settings (pour hébergeurs cloud)
# DATABASE_URL sera fourni automatiquement par l'hébergeur (Render, Railway, Heroku)
# Pour développement local avec MySQL :
DB_NAME=boursa_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=3306

# Security settings
ADMIN_URL=admin-panel/  # Changez pour sécurité
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Optionnel : Redis pour Celery (si utilisé)
REDIS_URL=redis://localhost:6379/0

# Email settings (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Redis settings (for Celery)
REDIS_URL=redis://localhost:6379/0

# External API keys (if needed)
# YAHOO_FINANCE_API_KEY=your_key_here
# ALPHA_VANTAGE_API_KEY=your_key_here